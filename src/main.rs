#[cfg(test)]
mod tests {
    #[test]
    fn addition() {
        assert_eq!(2, 1 + 1);
    }

    #[test]
    fn subtraction() {
        assert_eq!(0, 1 - 1);
    }
}

fn main() {
    println!("Hello, world!");
}
